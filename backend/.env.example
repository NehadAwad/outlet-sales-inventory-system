NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=pos_db

# Neon / managed Postgres: set DB_SSL=true (requires TLS)
DB_SSL=false

CORS_ORIGIN=http://localhost:5173
